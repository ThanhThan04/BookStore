namespace BookStore.Cost
{
    public static class Enums
    {
        public enum BorrowStatus
        {
            Borrowed,
            Returned
        }
        public enum UserRole
        {
            User,
            Admin
        }
    }
}
